import { parse, stringify } from '../index';

describe('sip-to-js-to-sip', () => {
    it.todo('parsing, then stingifying should end up back to the starting point');
});

describe('full example collection', () => {
    it.todo('should test a collection of tests for whether the outputs are correct');
});
