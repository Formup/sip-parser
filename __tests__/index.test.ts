import { parse, stringify } from '../index';

describe('full example collection', () => {
    describe('requests', () => {
        it.todo('should test a collection of requests for whether the outputs are correct');
    });
    describe('responses', () => {
        it.todo('should test a collection of responses for whether the outputs are correct');
    });
});
